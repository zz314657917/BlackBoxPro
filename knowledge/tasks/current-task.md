# 当前任务

## 本次已完成

- 基于当前代码扫描生成首版 `knowledge/`。
- 将项目地图、开发规则、构建验证、协议运行时、测试系统和现状偏差落库。
- 明确区分了“当前代码事实”和“旧文档历史描述”。

## 本次确认的关键结论

- 插件当前主链路是 HTTP 中继，不是旧文档中的 Plugin Message Channel。
- 根任务里没有 `mod_buildAll`，README/AGENTS 需要后续对齐。
- `ActionCatalog` 当前登记 `115` 个 action。
- `RuntimeBlackBoxConfig.update(...)` 当前未见调用，运行时默认配置基本靠代码快照。
- `/actions` 端点常量存在，但插件和客户端都还没实现。

## 后续最值得继续处理的点

- 对齐 README、AGENTS 与当前构建任务名、通信架构描述。
- 决定客户端运行时配置是否要真正落地到外部配置文件加载。
- 决定 `/actions` 是实现、还是删除未使用常量。
- 如果未来继续推进测试系统，优先统一 `runAll` 与 catalog 流程的职责边界。

## 更新时间

- 初始化时间：`2026-04-19`
