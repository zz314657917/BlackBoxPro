name: Build & Release

on:
  push:
    branches: [main, dev-2.0]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup JDK 8 (for 1.12.2)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 8

      # actions/setup-java 最后安装的版本会成为默认 JAVA_HOME
      # 需要把 JDK 21 设回默认，1.12.2 通过 Gradle toolchain 自动选择 JDK 8
      - name: Set JDK 21 as default
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            .gradle-user-home-root
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle.kts', '**/gradle.properties', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-${{ runner.os }}-

      - name: Grant execute permission
        run: |
          chmod +x gradlew
          chmod +x mod/1.12.2/gradlew
          chmod +x plugin/gradlew

      - name: Remove local proxy settings and fix CI compatibility
        run: |
          # 移除所有 gradle.properties 中的本地代理配置
          find . -name 'gradle.properties' -exec sed -i '/systemProp\.http\.proxy/d;/systemProp\.https\.proxy/d;/systemProp\.java\.net\.useSystemProxies/d;/systemProp\.http\.nonProxyHosts/d;/systemProp\.https\.nonProxyHosts/d' {} +
          # CI 已通过 setup-java 安装 JDK，不需要 foojay toolchain resolver
          sed -i '/foojay-resolver/d' mod/settings.gradle.kts

      - name: Read version
        id: version
        run: echo "version=$(grep '^version=' gradle.properties | cut -d= -f2)" >> "$GITHUB_OUTPUT"

      - name: Build all modules
        run: ./gradlew buildAll --no-daemon

      - name: List artifacts
        run: ls -la build/libs/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: BlackBoxPro-${{ steps.version.outputs.version }}
          path: build/libs/*.jar

  release:
    needs: build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev-2.0'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version
        id: version
        run: echo "version=$(grep '^version=' gradle.properties | cut -d= -f2)" >> "$GITHUB_OUTPUT"

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: BlackBoxPro-${{ steps.version.outputs.version }}
          path: artifacts/

      - name: Determine tag
        id: tag
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          if [ "${{ github.ref }}" = "refs/heads/dev-2.0" ]; then
            TAG="v${VERSION}-dev"
          else
            TAG="v${VERSION}"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "prerelease=${{ github.ref == 'refs/heads/dev-2.0' }}" >> "$GITHUB_OUTPUT"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: BlackBoxPro ${{ steps.tag.outputs.tag }}
          prerelease: ${{ steps.tag.outputs.prerelease == 'true' }}
          generate_release_notes: true
          files: artifacts/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
